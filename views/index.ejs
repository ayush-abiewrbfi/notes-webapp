<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Tasks</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gradient-to-br from-zinc-900 via-zinc-800 to-zinc-900 min-h-screen text-white font-sans">

  <header class="w-full sticky top-0 z-50 bg-zinc-900/70 backdrop-blur-md shadow-md">
    <div class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
      <h1 class="text-2xl font-semibold tracking-tight">Task Manager</h1>
    </div>
  </header>

  <main class="max-w-5xl mx-auto px-6 py-10">
    <section class="mb-12">
      <form action="/create" method="post" class="space-y-4 bg-zinc-800/40 backdrop-blur-lg p-6 rounded-lg shadow-md">
        <input class="w-full px-4 py-3 rounded-md bg-zinc-700/60 backdrop-blur-sm text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-600" type="text" placeholder="Title goes here" name="title" required>
        <textarea class="w-full px-4 py-3 rounded-md bg-zinc-700/60 backdrop-blur-sm text-white placeholder-gray-400 resize-none focus:outline-none focus:ring-2 focus:ring-blue-600" name="details" placeholder="Task details go here" rows="5" required></textarea>
        <input class="bg-blue-600 hover:bg-blue-700 transition-colors text-white font-medium px-5 py-2 rounded-md cursor-pointer" type="submit" value="Create Task">
      </form>
    </section>

    <section>
      <h2 class="text-xl font-semibold mb-6">Your Tasks</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
        <% if (files.length > 0) { %>
          <% files.forEach(function(value) { %>
            <div class="bg-zinc-800/40 backdrop-blur-md p-4 rounded-lg shadow-md transition hover:shadow-lg">
              <% 
                let displayTitle = value.replace(/\.txt$/i, ''); 
                %>
                <h3 class="text-white text-xl font-semibold truncate select-text"><%= displayTitle %></h3>

              <div class="flex flex-col space-y-2 text-sm">
                <a href="/files/<%= value %>" class="text-blue-400 hover:underline">Read More</a>
                <a href="/edit/<%= value %>" class="text-green-400 hover:underline">Edit</a>
                <a href="/delete/<%= value %>" class="text-red-400 hover:underline">Delete</a>
              </div>
            </div>
          <% }) %>
        <% } else { %>
          <p class="text-gray-400">No tasks yet.</p>
        <% } %>
      </div>
    </section>
  </main>
</body>
</html>
